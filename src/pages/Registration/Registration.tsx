import React, { type FC, type JSX } from 'react';

import { RegistrationContainer } from '@features/auth/RegistrationContainer/RegistrationContainer';

export const Registration: FC = (): JSX.Element => <RegistrationContainer />;
